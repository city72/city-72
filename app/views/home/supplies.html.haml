= javascript_include_tag "supplies"
.main-container.things
  = render :partial => "partials/header"
  .intro
    .photo-overlay
      %h2.mobile-title Supplies
      %h2 Get your kit together.
      %p People are most resilant in emergencies when they are connected to others. Getting prepared for emergencies is simple and effective when you do it with friends and family. There are lots of ways to get connected. Choose the one that’s best for you
  .action-bar
    %ul.tabs
      %li.essentials-trigger.active
        %a{href:"#tab-essentials-content"} Essentials
      %li
        %a{href:"#tab-useful-content"} Useful
      %li
        %a{href:"#tab-personal-content"} Personal
    .list-download
      %a Download full list
  %ul.tabs-content 
    %li.tab-essentials-content
      %p.tab-tip A good rule of thumb is to have supplies for about 3 days, or 72 hours.
      %ul.things-list.essential
        - @items.select {|item| item.category == "#{Categories::ESSENTIAL.to_s}" }.each do |item|
          %li
            .use-case-dialog
              #{item.use_case}
            %img{src: "#{item.image}"}
            %h4 #{item.title}
            %p.overlay-text #{item.use_case}
      %ul.things-list.useful
        - @items.select {|item| item.category == "#{Categories::USEFUL.to_s}" }.each do |item|
          %li
            .use-case-dialog
              #{item.use_case}
            %img{src: "#{item.image}"}
            %h4 #{item.title}
            %p.overlay-text #{item.use_case}
      %ul.things-list.personal
        - @items.select {|item| item.category == "#{Categories::PERSONAL.to_s}" }.each do |item|
          %li
            .use-case-dialog
              #{item.use_case}
            %img{src: "#{item.image}"}
            %h4 #{item.title}
            %p.overlay-text #{item.use_case}
    %li.tab-useful-content
    %li.tab-personal-content
  .kits
    %h2 Things to share
    %p No worries, we’ve got your backpack. Below you’ll find some pre-assembled kits you can buy directly from amazon.com
    %ul.kits-list
      - @kits.each do |kit|
        %li
          %a{href: "#", onClick:"window.open('#{kit.url}', '', 'width=600,height=500')"}
            %img{src: "#{kit.image}"}
            %span #{kit.title}
= render :partial => "partials/footer"
.main-content.map
  .screen-intro
    %p.screen-info Embed a custom Google Crisis Map for your area.
    .action-area
      %a.btn.btn-primary{:href => "#"} Save Changes

  %h2 Instructions to get the embed link:
  %ol 
    %li Set up your map (see <a href="#">instructions on city72tookit site</a>).
    %li Click the "Share" button to see the map links.
    %li  Copy the "Link" code to paste here.

  %form{:role => "form"}
    .form-group
      %label{:for => "about-image"} Crisis Map Link <span class="help">(starts with "http://")</span>
      %input.form-control#about-image{:type => "text"}/
      
  .form-actions
    %input.btn.btn-primary{:type => "submit", :value => "Save Changes"}/


  %form{:role => "form"}
    %h2 Official Updates
    .form-group
      %label{:for => "your-feed"} Your organization's Official Twitter feed <span class="help">(e.g. @SF_emergency)</span>
      %input.form-control#your-feed{:type => "text"}/
    
    / %h2 Partner Updates
    .form-group
      %label{:for => "partner-feed"} Partner Twitter feeds <span class="help">(e.g. @USGS, @RedCrossBayArea)</span>
      %input.form-control#partner-feed{:type => "text"}/

    / %h2 Transportation Updates
    .form-group
      %label{:for => "transportation-feed"} Relevant transport Twitter feeds <span class="help">(e.g. @511sfbay)</span>
      %input.form-control#transportation-feed{:type => "text"}/

  .form-actions
    %input.btn.btn-primary{:type => "submit", :value => "Save Changes"}/
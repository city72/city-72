class RenameVideos < ActiveRecord::Migration
  def change
    # Story.all.each do |story|
    #   story.video_url = "http://vimeo.com/#{story.video_url.split('/')[-1]}"
    #   story.save
    # end
  end
end
